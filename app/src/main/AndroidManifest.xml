<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mudraapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".NavigationActivity"
            android:label="@string/title_activity_navigation"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".BottomActvity"
            android:label="@string/title_activity_bottom_actvity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".ApplyIntent" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ReferEarn" />
        <activity android:name=".EMIcalculator" />
        <activity android:name=".ContactUsForm" />
        <activity android:name=".ContactUs" />
        <activity android:name=".KnowledgeCenter" />
        <activity android:name=".ApplyActivity" />
        <activity android:name=".CheckScore" />
        <activity android:name=".OptionsActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>